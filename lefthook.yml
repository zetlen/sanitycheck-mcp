pre-commit:
  parallel: true
  commands:
    lint:
      glob: "*.{js,ts}"
      run: npx oxlint {staged_files}
    format:
      glob: "*.{js,ts,json}"
      run: npx oxfmt --check {staged_files}

commit-msg:
  commands:
    commitlint:
      run: npx commitlint --edit {1}
